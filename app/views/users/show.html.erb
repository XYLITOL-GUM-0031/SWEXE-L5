<h1><%= @user.name || @user.uid %> のプロフィール</h1>

<p><strong>自己紹介：</strong><%= @user.bio.presence || "未登録" %></p>
<p><strong>登録日：</strong><%= @user.created_at.strftime("%Y年%m月%d日") %></p>
<p><strong>ツイート数：</strong><%= @tweets.count %></p>

<% if session[:login_uid] == @user.uid %>
  <%= link_to "プロフィール編集", edit_user_path(@user) %>
<% end %>

<h2>ツイート一覧</h2>
<% @tweets.each do |tweet| %>
  <p><%= tweet.message %>（<%= tweet.created_at.strftime("%m/%d %H:%M") %>）</p>
<% end %>
